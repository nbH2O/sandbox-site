<div class="flex flex-col gap-2">
    <x-input size="lg" placeholder="{{ __('Name') }}" />
    <x-input size="lg" placeholder="{{ __('Password') }}" />
    <x-input size="lg" placeholder="{{ __('Confirm Password') }}" />
</div>
